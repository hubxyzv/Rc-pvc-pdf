services:
  - type: web
    name: rc-pvc-propdf
    env: node
    plan: free
    # 🛠️ Build command installs dependencies and then the Chrome browser for Puppeteer
    buildCommand: npm install && npx puppeteer browsers install chrome
    # 🚀 Start command to run your server
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
      - key: MONGODB_URI
        value: "mongodb+srv://shivamdrive1234_db_user:sBdRvWk8cRyiDzj7@cluster0.ixk2kuh.mongodb.net/?appName=Cluster0"
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: "false"
    # 🛡️ Optimized for Render's environment
    healthCheckPath: /
    autoDeploy: true

# Blueprint for automatic environment setup
